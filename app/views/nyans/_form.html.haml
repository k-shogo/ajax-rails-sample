= simple_form_for @nyan, :html => { :class => 'form-horizontal' } do |f|
  = f.input :_id
  = f.input :title
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), nyans_path, :class => 'btn'
